<?xml version="1.0" encoding="utf-8"?>
<!-- Ripple wrapper for bg_xnet_audio_control_button -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/xnetAccentPrimarySoft">
    <item android:id="@android:id/mask">
        <!-- Mask matches the octagonal cut shape of the button -->
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white"/>
        </shape>
    </item>
    <item android:drawable="@drawable/bg_xnet_audio_control_button"/>
</ripple>
